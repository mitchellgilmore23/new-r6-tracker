const axios = require('axios');
export const $ = require("jquery"); window.$= $

export function main (lookupName,currentPlayerCol,lookupPlatform,array) {
console.log(lookupName,currentPlayerCol,lookupPlatform,array)
}
export function matches(lookupName,currentPlayerCol,lookupPlatform,array) {
}
export function seasons(lookupName,currentPlayerCol,lookupPlatform,array) {
}